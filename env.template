# Jukebox Configuration Template
# Copy this to .env and customize for your setup

# === HARDWARE MODE ===
# Set to false for headless/development mode without physical hardware
# Set to true for full hardware operation with display, RFID, buttons
HARDWARE_MODE=true

# === ILI9488 DISPLAY SPI GPIOs ===
# SPI chip select (CS), data/command (DC), and reset (RST) pins for display
DISPLAY_GPIO_CS=8
DISPLAY_GPIO_DC=23
DISPLAY_GPIO_RST=24

# === SUBSONIC CONFIGURATION ===
# Required for music streaming (Navidrome/Subsonic-compatible servers)
# Note: These names align with the app's current config
SUBSONIC_URL=http://your-subsonic-server:4747
SUBSONIC_USER=your-username
SUBSONIC_PASS=your-password

# Optional: If your public Subsonic domain is behind Basic Auth, set a LAN-only base for Chromecast
# so Cast devices fetch streams directly from LAN without needing Basic.
# Example: http://192.168.68.102:4747 or https://gonic.lan
# Leave empty to use SUBSONIC_URL for all stream URLs.
# SUBSONIC_CAST_BASE_URL=http://192.168.68.102:4747

# === OPTIONAL: DATABASE ===
# SQLite database path (relative to project root)
DATABASE_PATH=jukebox.db

# === OPTIONAL: CHROMECAST ===
# List of all available Chromecast devices in your home (comma-separated)
CHROMECAST_DEVICES=Living Room,Bedroom,Kitchen,TV lounge

# Default Chromecast device name for audio output (must be in CHROMECAST_DEVICES list)
DEFAULT_CHROMECAST_DEVICE=Living Room

# Fallback devices to try if primary device is offline (comma-separated, in priority order)
CHROMECAST_FALLBACK_DEVICES=Bedroom,Kitchen

# === OPTIONAL: FILE PATHS ===
# Album cover cache directory
STATIC_FILE_PATH=album_covers

# === DEVELOPMENT MODE EXAMPLE ===
# For headless operation on clean systems:
# HARDWARE_MODE=false
# SUBSONIC_BASE_URL=http://demo.subsonic.org
# SUBSONIC_USERNAME=guest
# SUBSONIC_PASSWORD=guest

# === SECURITY (PUBLIC API) ===
# Strong API key required for all /api/* endpoints when exposed publicly
# API_KEY=change-me-to-a-long-random-string

# Comma-separated list of allowed origins for browsers (CORS)
# CORS_ALLOW_ORIGINS=https://api.example.com,https://app.example.com

# Comma-separated list of allowed Host headers
# ALLOWED_HOSTS=api.example.com

# Redirect HTTP -> HTTPS at app (use behind TLS proxy)
# ENABLE_HTTPS_REDIRECT=true

# Allow localhost (127.0.0.1) to call API without API_KEY if API_KEY is not set (server-side calls)
# ALLOW_LOCAL_API_BYPASS=true

# Expose Prometheus metrics without auth (default false)
# ALLOW_PUBLIC_METRICS=false

# Public base URL of the jukebox (used to build absolute URLs for Chromecast metadata like cover art)
# Example: https://jukeplayer.example.com
# PUBLIC_BASE_URL=https://jukeplayer.example.com

# === API DOCS (Swagger/OpenAPI) ===
# Enable interactive API docs at /docs without enabling full DEBUG mode.
# Only enable when access is protected (e.g., Basic Auth at reverse proxy).
# ENABLE_DOCS=false
# DOCS_URL=/docs
# OPENAPI_URL=/openapi.json
